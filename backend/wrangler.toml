# Deployment configuration for Cloudflare Workers
# 使用 workers-rs 或 Python Workers

name = "ai-daily-api"
main = "app/main.py"
compatibility_date = "2024-01-01"

[build]
command = "pip install -r requirements.txt"

[env.production]
ENV = "production"

[env.staging]
ENV = "staging"
